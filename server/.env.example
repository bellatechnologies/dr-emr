# Copy to server/.env and fill in from Project Settings -> API in your Supabase dashboard.
# SUPABASE_SERVICE_ROLE_KEY bypasses Row Level Security -- server-side only, never in git,
# never sent to the browser.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
